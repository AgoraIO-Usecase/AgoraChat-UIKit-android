package io.agora.uikit.feature.thread.interfaces

import io.agora.uikit.feature.thread.widgets.EaseChatThreadRole

interface OnChatThreadRoleResultCallback {
    fun onThreadRole(role:EaseChatThreadRole)
}